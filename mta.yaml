_schema-version: "3.2"
ID: employeeList
version: 0.0.1
modules:
- name: employeelist-approuter
  type: approuter.nodejs
  path: employeelist-approuter
  requires:
  - name: employeeList_html_repo_runtime
  - name: dest_employeeList
  parameters:
    disk-quota: 256M
    memory: 256M
- name: employeeList_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: employeeList_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - EmployeeList-content.zip
      name: EmployeeList
      target-path: resources/
- name: EmployeeList
  type: html5
  path: EmployeeList
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: employeeList_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: employeeList_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: dest_employeeList
  type: org.cloudfoundry.managed-service
  parameters:
    service: destination
    service-plan: lite
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
